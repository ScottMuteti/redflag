{
  "name": "redflag",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "simulation-engine",
    "portal"
  ],
  "scripts": {
    "lint": "npm run lint --workspace=simulation-engine && npm run lint --workspace=portal",
    "test": "npm test --workspace=simulation-engine"
  }
}
